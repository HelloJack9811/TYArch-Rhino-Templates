# TYARCH Rhino 建模指引

| 项目 | 内容 |
| --- | --- |
| 仓库地址 | <https://github.com/HelloJack9811/TYArch-Rhino-Templates> |
| 文档版本 | v0.7 |
| 适用模板 | `TYARCH-general template-4.0.3dm` |
| 基准单位 | 毫米（Millimeters） |
| 绝对容差 | 0.01 mm |
| 角度容差 | 1.0° |
| 适用对象 | 团队建模人员、AI 建模助手（RhinoMCP） |

> **本稿状态**：三章内容均已成稿。图层部分只展开到二级图层（即 `1-ARCH::构件` 所在的层级）。

## 目录

- [1. 使用说明](#1-使用说明)
- [2. 图层说明](#2-图层说明)
- [3. 材质命名与图块命名](#3-材质命名与图块命名)

---

## 1. 使用说明

### 1.1 文档目的

本指引是 TYARCH 团队在 Rhino 中协作建模的命名与组织依据，服务两类使用者：

- **团队成员**：新建、修改、交付 Rhino 文件时，参考本指引确定几何体放在哪个图层、图块如何归属、材质如何命名。
- **AI 建模助手**：通过 RhinoMCP 等通道读写 Rhino 文件时，本指引是图层、材质、图块命名的机器可读依据。AI 在开始建模前宜先读本指引，不凭猜测命名。

### 1.2 首要原则

本指引的目的是**让人和 AI 都能读懂模型里有什么**。形式一致服务于这个目的，而不是反过来。

- **可读优先于合规**：名称语义清楚、读得懂，即使格式与本文档的约定有出入，也是可理解、可接受的。
- **约定按"尽量"执行**：人的记忆与注意力有限，命名时形式上偶有疏漏，只要不影响语义，不必回头返工。
- **只有伤及可读性的错误需要返工**：例如漏写段分隔符、段内漏写空格导致词语粘连（`woodenfinishing`）、明显的笔误。这类错误会让别人和 AI 无法正确切分字段，应当修正。

因此本文档中的"建议""尽量""约定"都按上述原则理解。判断一个名字是否合格，标准是**结果是否可读、合理**，而不是是否逐条对齐格式。

### 1.3 基准环境

| 项目 | 取值 | 说明 |
| --- | --- | --- |
| 模板文件 | `TYARCH-general template-4.0.3dm` | Rhino 的默认模板设置已指向该文件 |
| 建模单位 | 毫米 | 不建议在设计中途切换单位 |
| 绝对容差 | 0.01 | 与模板一致，不建议放宽 |
| 角度容差 | 1.0° | — |
| 相对容差 | 1.0% | — |
| 线型 | `Continuous` / `Hidden` / `Dashed` / `DashDot` / `Center` / `Dots` / `$-Redline` | 模板内置，按需引用 |
| 标注样式 | `ARCH Style-100` / `ARCH Style-50` / `ARCH Style-20` | 模板内置 |

新文件建议一律从模板新建，而不是复制旧文件后改内容，以免把旧文件的图层与材质残留一并带入。

**新机器的环境搭建**（模板与显示模式如何装进 Rhino）见配套的 `TYARCH-Rhino环境部署.md`；本文件只负责装好之后怎么建模。

### 1.4 图层命名语法

图层名以双冒号 `::` 分层：

```
[分区编号]-[专业]::[二级分类]::[三级分类]
```

- 首段为分区编号加专业，如 `1-ARCH`；其后每两个冒号下沉一层，层级数即对象归属的细分深度。
- ASCII 前缀（`0`–`4`、`X`/`Y`/`Z`、`#`）负责排序与机器识别，中文负责语义描述。
- 段内建议不用空格、全角标点、斜杠、点号，便于导入导出与脚本解析。
- 同级命名建议唯一。用 `1`、`副本`、`new` 这类后缀区分同类图层，会让后续读取和筛选变得困难。

本语法只约束图层。材质与图块各自独立命名，均不派生于图层命名：材质见 3.1，图块见 3.2。

### 1.5 团队协作建议

1. **结果优先，不限定过程**：先建模再从 `#-DRAFT` 归类，或先选图层再建模，都是个人习惯，本指引不作规定；建议保证交付结果中对象的图层归属清晰、命名可读。
2. **结构开放**：图层体系是开放结构，可按需在任一节点下新增子层，建议遵循 1.4 的语法。当前分类中缺失的类别属于正常状态，按需补建即可，不必为"齐备"而补建。既有一级、二级图层建议保持名称与归属稳定；如需重命名或删除，建议先与团队沟通。
3. **一物一层**：对象层面的图层归属建议唯一，不用隐藏/显示代替专业区分。
4. **过程稿隔离**：方案推敲阶段的几何建议放进 `#-DRAFT` 分支，交付前清理或归位，避免过程线混入成图。
5. **交付前自检**：建议核对单位与容差，确认没有对象落在 `0` 图层。

### 1.6 AI 协作建议

1. **先读后写**：动手前先读本指引与当前文档图层表；可按需新增图层，建议遵循 1.4 的命名语法。
2. **命名即契约**：既有图层、材质、图块名称是稳定接口，不建议擅自重命名或删除。
3. **改动前请示**：删除图层、删除对象、清空文档、修改模板、新增一级分区，建议先经人工确认。
4. **结果可核对**：每次改动后报告对象数量、所在图层、使用的材质与图块名，便于人工核对。
5. **不确定则停下**：几何体无法明确归类时，放到语义最接近的分区节点，并在交付说明中标注；确实缺失的分类可按语法新建图层。
6. **不过度纠正**：遇到大小写、字段简写一类形式差异，语义清楚即不必"修正"，避免把人写的名字改得面目全非。

**需要返工的只有一种情况**：名字已经读不懂了（字段粘连、分隔符缺失、明显笔误）。此外都不要因为格式不齐而回退或改名。

### 1.7 修改与版本

- 本指引的变更由模板维护人统一提交，团队成员不单独改本文件。
- 每次模板版本升级（如 4.0.3 → 4.0.4）同步更新「适用模板」与受影响的图层条目。
- 新增一级分区属于结构性变更，建议先讨论、再落文档，最后才改模板。

---

## 2. 图层说明

> 本章的图层名称、显示色、线型、打印线宽、材质绑定由当前文档与模板文件实测得出；各图层的**用途描述**由名称与层级关系推断而来，后续人工 review 时简要微调即可。本章只展开到二级图层。

### 2.1 图层结构

图层体系是**开放性结构**，本节描述的是分组逻辑，不是封闭清单：

- 使用者（无论员工或 AI）可以按需在任一节点下新增子层，按 1.4 的语法命名即可。
- 当前分类中缺失的类别属于正常状态，按需补建，不必为"齐备"而补建。
- 因此本文档不统计图层数量、不设层级深度上限，图层规模随项目需要变化。

结构上分为三类：

- **专业分区**（`0`–`4`）：承载设计本体，按专业流转顺序编号。
- **辅助分区**（`X` / `Y` / `Z`）：不承载设计本体，分别是外部参照、出图辅助、其他。
- **草稿分区**（`#`）：过程稿专用。`#` 在 ASCII 中排在最前，图层列表中会集中显示在最上方，便于集中清理。

### 2.2 一级图层

一级图层决定专业归属与出图分组，在结构上均为**分区节点**；放置约定见 2.4。

| 一级图层 | 含义 | 当前包含的二级分类 |
| --- | --- | --- |
| `0` | Rhino 系统默认层 | — （建议保持空白，不放几何体） |
| `0-SITE` | 场地 | 道路、地面、地形、水体 |
| `1-ARCH` | 建筑 | 结构、空间、完成面、幕墙、构件、门窗、屋顶 |
| `2-INTR` | 室内 | 地面、墙面、天花、线脚、固定家具、活动家具、门窗 |
| `3-LAND` | 景观 | 地面、挡墙、道路、水体、绿地 |
| `4-EQPM` | 设备 | 照明、交通、暖通 |
| `X-REF` | 外部参照 | 轴、参照、退距线 |
| `Y-DRWG` | 出图辅助 | 图元、标注、视口、剖切 |
| `Z-MISC` | 其他 | — （兜底分类） |
| `#-DRAFT` | 草稿 | 草图、轮廓线 |

`0`、`X-REF`、`Y-DRWG`、`Z-MISC`、`#-DRAFT` 及其子层当前未绑定图层材质，五个专业分区图层已绑定白模材质，见 2.7。

### 2.3 二级图层

二级图层是建模时的实际操作层。"层级角色"一列描述该层在当前结构中的状态，不代表对放置方式的限制——放置约定见 2.4。

#### 2.3.1 `0-SITE` 场地

| 二级图层 | 层级角色 | 显示色 | 打印线宽 | 说明 |
| --- | --- | --- | --- | --- |
| `0-SITE` | 分区节点 | — | 0.18 | 场地总控 |
| `0-SITE::道路` | 末级图层 | `#BEBEBE` | 0.18 | 场地内道路、车行与人行通道 |
| `0-SITE::地面` | 末级图层 | `#BEBEBE` | 0.18 | 场地地坪、硬质铺装面 |
| `0-SITE::地形` | 末级图层 | `#BEBEBE` | 0.18 | 原始与设计地形曲面 |
| `0-SITE::水体` | 末级图层 | `#3FBFBF` | 0.18 | 场地水面 |

#### 2.3.2 `1-ARCH` 建筑

| 二级图层 | 层级角色 | 显示色 | 打印线宽 | 说明 |
| --- | --- | --- | --- | --- |
| `1-ARCH` | 分区节点 | — | 0.18 | 建筑总控 |
| `1-ARCH::结构` | 分区节点 | — | **0.35** | 结构构件总控，出图时按粗线强调；下分柱、梁、楼板、结构墙 |
| `1-ARCH::空间` | 分区节点 | — | 0.18 | 空间界定体量；下分墙 |
| `1-ARCH::完成面` | 分区节点 | — | 0.18 | 建筑完成面；下分地面、墙面、装饰件（子层线宽 0.13） |
| `1-ARCH::幕墙` | 分区节点 | — | 0.18 | 幕墙系统；下分构件、装饰板、装饰件 |
| `1-ARCH::构件` | 分区节点 | — | 0.18 | 通用建筑构件；专用构件下移三层，如楼梯、栏杆 |
| `1-ARCH::门窗` | 分区节点 | — | 0.18 | 建筑门窗总控；下分门、窗、玻璃 |
| `1-ARCH::屋顶` | 末级图层 | `#FFC68C` | 0.18 | 屋面、屋脊、天窗基面 |

`1-ARCH::构件` 是分区节点兼通用层的典型：不属于楼梯、栏杆等专用子类的构件，可直接放在该层，也可按需再建子层。

#### 2.3.3 `2-INTR` 室内

| 二级图层 | 层级角色 | 显示色 | 打印线宽 | 说明 |
| --- | --- | --- | --- | --- |
| `2-INTR` | 分区节点 | — | 0.18 | 室内总控 |
| `2-INTR::地面` | 末级图层 | `#556080` | 0.18 | 室内地面完成面 |
| `2-INTR::墙面` | 末级图层 | `#3F60BF` | 0.18 | 室内墙面完成面 |
| `2-INTR::天花` | 末级图层 | `#8090BF` | 0.18 | 室内吊顶与天花造型 |
| `2-INTR::线脚` | 分区节点 | `#00FFFF` | 0.18 | 装饰线脚；下分踢脚线、装饰线（与其子层同色） |
| `2-INTR::固定家具` | 末级图层 | `#B3BFE6` | 0.18 | 固定家具、定制柜体 |
| `2-INTR::活动家具` | 末级图层 | `#E6E6E6` | 0.18 | 活动家具、软装陈设 |
| `2-INTR::门窗` | 分区节点 | — | 0.18 | 室内门窗总控；下分门、窗、玻璃 |

#### 2.3.4 `3-LAND` 景观

| 二级图层 | 层级角色 | 显示色 | 打印线宽 | 说明 |
| --- | --- | --- | --- | --- |
| `3-LAND` | 分区节点 | — | 0.18 | 景观总控 |
| `3-LAND::地面` | 末级图层 | `#696969` | 0.18 | 景观铺装与硬质地面 |
| `3-LAND::挡墙` | 末级图层 | `#AFDE39` | 0.18 | 挡土墙、景墙 |
| `3-LAND::道路` | 末级图层 | `#BEBEBE` | 0.18 | 景观步道、园路 |
| `3-LAND::水体` | 末级图层 | `#3FBFBF` | 0.18 | 景观水面、水池 |
| `3-LAND::绿地` | 末级图层 | `#007F00` | 0.18 | 草坪、绿化种植区 |

#### 2.3.5 `4-EQPM` 设备

| 二级图层 | 层级角色 | 显示色 | 打印线宽 | 说明 |
| --- | --- | --- | --- | --- |
| `4-EQPM` | 分区节点 | — | 0.18 | 设备总控 |
| `4-EQPM::交通` | 末级图层 | `#FFD27F` | 0.18 | 电梯、扶梯、自动步道 |
| `4-EQPM::暖通` | 末级图层 | `#FFF68F` | 0.18 | 暖通设备与风口 |
| `4-EQPM::照明` | 末级图层 | `#FF8C69` | 0.18 | 灯具与照明设备 |

#### 2.3.6 `X-REF` 外部参照 / `Y-DRWG` 出图 / `#-DRAFT` 草稿

`Z-MISC` 当前没有二级图层，暂不使用。

| 二级图层 | 层级角色 | 显示色 | 线型 / 线宽 | 说明 |
| --- | --- | --- | --- | --- |
| `X-REF::轴` | 末级图层 | `#0000BF` | DashDot / 0.13 | 轴网；用点划线，出图弱化 |
| `X-REF::参照` | 末级图层 | — | Continuous / 0.18 | 底图、参照几何 |
| `X-REF::退距线` | 末级图层 | — | Continuous / 0.18 | 建筑退距控制线 |
| `Y-DRWG::图元` | 末级图层 | — | Continuous / 0.18 | 出图用图面元素 |
| `Y-DRWG::标注` | 分区节点 | — | Continuous / 0.18 | 标注总控；下分数值、引注 |
| `Y-DRWG::视口` | 末级图层 | — | Continuous / 0.18 | 图纸视口与图框 |
| `Y-DRWG::剖切` | 末级图层 | — | Continuous / **0.5** | 剖切线与剖切面，最粗线宽强调 |
| `#-DRAFT::草图` | 末级图层 | — | Continuous / 0.18 | 过程草图 |
| `#-DRAFT::轮廓线` | 末级图层 | — | Continuous / 0.18 | 过程轮廓线 |

### 2.4 几何体与图块的归属约定

| 情形 | 约定 |
| --- | --- |
| 常规几何体 | 建议放在语义最贴合的图层；该层当前是否已有子层，不作为硬性限制。 |
| 分区节点 | **可以**直接存放独立物件，但**不建议**——会影响后续按层筛选与出图控制。 |
| 图块定义 | 当图块的子物件跨越多个图层时（例如模块化立面同时涉及墙面、窗、玻璃、栏杆），**允许将图块放入语义最接近的节点图层**。 |
| 归类不明 | 放到语义最接近的分区节点，并在交付说明中标注；建议不为单个对象新建分类。 |

### 2.5 图层属性约定

| 属性 | 约定 |
| --- | --- |
| 显示色 | 当前结构中，只有末级图层定义实际显示色，上级分区节点统一为 `#000000` 占位。出图颜色由末级层决定。 |
| 打印线宽 | 常规 0.18；结构构件 0.35；`1-ARCH::完成面` 子层与轴网 0.13；剖切 0.5，分层级表达轻重关系。 |
| 线型 | 默认 `Continuous`，当前仅轴网使用 `DashDot`。其他线型按需引用模板内置线型，不建议新建线型。 |
| 可见性与锁定 | 模板中全部图层默认为可见、未锁定。图层可见性属于工作状态，不属于交付内容。 |
| 材质来源 | 专业分区图层使用"依图层"材质，已绑定白模材质，见 2.7；辅助分区图层未绑定图层材质。新材料命名见 3.1。 |

### 2.6 图层表维护

图层表中可能残留已删除的惰性图层记录，对建模与出图没有影响。正式使用中不建议通过 Purge 清理图层。

### 2.7 模板现状：白模材质

模板中的 52 个材质记录**逐项完全相同**，是同一个白模材质：

| 属性 | 取值 |
| --- | --- |
| 名称 | `DEFAULT-general white` |
| 漫反射色 | `#FAFAFA` |
| 高光色 | `#FFFFFF` |
| 光泽度 / 反射率 / 透明度 | 0 / 0 / 0 |
| 折射率 | 1.0 |
| 贴图 | 无 |

这个材质是无贴图的平光白色，用于白模阶段：52 个专业分区图层各自绑定其中一条记录，而 Rhino 新建对象的材质来源默认就是"依图层"，因此**在白模阶段不需要逐对象赋材质，模型可以直接进渲染器**。

| 分区 | 材质记录数 |
| --- | --- |
| `0-SITE` | 5 |
| `1-ARCH` | 24 |
| `2-INTR` | 13 |
| `3-LAND` | 6 |
| `4-EQPM` | 4 |

辅助分区（`0`、`X-REF`、`Y-DRWG`、`Z-MISC`、`#-DRAFT`）的图层未绑定图层材质。

由于每个图层各持有一条独立记录，后续把某个图层换成真实材质时不会牵动其他图层。

`DEFAULT-general white` 是模板的现状名，含义为"物理类型 default 的通用白色、未细分到子特性"，本身符合 3.1 的规则，可直接沿用。

---

## 3. 材质命名与图块命名

### 3.1 材质命名

材质名以连字符 `-` 分段，前两段为基本结构，其后按需追加：

```
[物理类型]-[特性或子类型]-[子特性]
```

| 段位 | 作用 | 大小写 |
| --- | --- | --- |
| `[物理类型]` | 材质所属的物理类型，如 `WOOD`、`STONE`、`DEFAULT` | 建议全大写 |
| `[特性或子类型]` | 该类型下的具体做法或子类 | 尽量全小写 |
| `[子特性]` | 区分同类材质时最关键的可辨识特征 | 尽量全小写 |
| 后续段位 | 需要更细区分时按同一思路继续追加 | 尽量全小写 |

物理类型建议全大写——它是区分"类型"与"字段"的主要视觉线索，写成小写会削弱可读性；其余段位尽量全小写，但大小写形式不作硬性要求。

| 示例 | 解读 |
| --- | --- |
| `WOOD-wooden finishing-original` | 物理类型为 wood；是一种木饰面完成面；最关键的可区分特征是原木色 |
| `STONE-marble-green` | 物理类型为 stone；是一种大理石；颜色为绿色 |
| `DEFAULT-general white` | 物理类型为 default；通用白色，未细分到子特性 |

写法：

- 段内多词用**空格**分隔，例如 `wooden finishing`。
- 长单词可用**点号**简写，例如 `perf.` = `perforated`。
- 段内不用连字符；依赖连字符的复合词（如 `anti-slip`）建议改写或避开。
- 缩写可用全大写，例如 `PVC`、`UV`。
- 首字母大写或驼峰（如 `Wooden Finishing`、`MarbleGreen`）属于书写习惯，语义不变即无需修改。

物理类型是**开放列表**。`DEFAULT`、`WOOD`、`STONE` 是目前用到的几种，不构成上限；新的物理类型按同一思路命名即可直接使用，不需要预先登记。

同一文件内材质名唯一。材质命名独立于图层命名，两者不互相派生，也不要求一一对应。

### 3.2 图块命名

图块命名采用**开放式多段结构**：首段为分区编号，其后是由总体到具体的若干描述字段。

```
[分区编号]-[字段1]-[字段2]-*
```

- `[分区编号]`：沿用图层分区编号的规定，`0`–`4` 对应五个专业分区。
- 其后各字段：建议由总体到具体书写，例如 所属系统 → 具体构件 → 规格编号或类型。
- `*` 表示字段数量开放，按需增减；本指引不限定字段总数，也不限定每段的具体含义。

| 示例 | 分区 | 解读 |
| --- | --- | --- |
| `1-#1-curt. wall-mullion-module a` | 建筑 | `#1` 号建筑幕墙上的竖挺，类型为 A 类模块部件 |
| `2-window-c1521-a` | 室内 | 窗类组件，以国标窗表编号 `c1521` 命名（宽约 1500、高约 2100），文件内识别类型为 `a` |

写法：

- 段内多词用空格分隔，长单词可用点号简写（如 `curt. wall` = `curtain wall`），与 3.1 一致。
- 段内不用连字符。
- 尽量全小写，大小写形式不作硬性要求。
- 字段语言不限，中英文均可，以读得懂为准。

**分区编号的含义**：分区编号只用来区分图块类型，**不绑定图块实际所在的图层**；它表达的是"这是哪一类图块"，而不是"它放在哪里"。这与 2.4 允许跨图层图块放入语义最接近节点是一致的。

- 例：亭子打包为建筑类 `1-pavillion`，实际放在 `3-LAND` 中，表达它是景观部分的构筑物。

**关于 `#`**：`#` 在首段与字段中的含义不同——首段是分区编号（沿用图层的 `#-DRAFT` 草稿分区），字段中的 `#` 是编号。位置本身即可区分，不必额外限制。

文件内图块名唯一（Rhino 本身也不允许图块定义重名）。

### 3.3 三套命名的字符约定对照

三套命名相互独立，不互相派生。对照如下，便于快速回忆，不作交叉约束：

| 项目 | 图层 | 材质 | 图块 |
| --- | --- | --- | --- |
| 分段方式 | `::` | `-` | `-` |
| 首段含义 | 分区编号 + 专业 | 物理类型 | 分区编号 |
| 段内空格 | 建议不用 | 允许 | 允许 |
| 长词点号简写 | 建议不用 | 允许 | 允许 |
| 段内连字符 | 首段内含（`1-ARCH`） | 不用 | 不用 |
| 大小写 | ASCII 前缀大写 | 类型建议大写，其余尽量小写 | 尽量小写 |

---

*本文档中的图层名称、颜色、线宽与材质信息以 `TYARCH-general template-4.0.3dm` 为准。本文档属于 TYARCH Rhino 工作环境仓库：<https://github.com/HelloJack9811/TYArch-Rhino-Templates>（本项目的长期仓库地址）。*
