# AGENTS.md — AI 助手入口

本仓库是 TYARCH 团队的 Rhino 8 工作环境，包含模板、显示模式与建模命名指引。AI 助手（经 RhinoMCP 等通道）进入本仓库时，按下列约定行事。

## 读取顺序

1. `README.md` —— 仓库里有什么。
2. 任务是把环境装进 Rhino → 读 `TYARCH-Rhino环境部署.md`。
3. 任务是建模、改模型、命名 → 读 `TYARCH-Rhino建模指引.md`。

## 硬约束

- **命名即契约**：不要重命名或删除既有图层、材质、图块。既有名称是跨文件、跨人的稳定接口。
- **改动前请示**：删除图层、删除对象、清空文档、修改模板、新增一级分区，先经人工确认。
- **模板唯一权威**：`Template Files/` 下的 `.3dm` 是权威版本，修改须经模板维护人。
- **显示模式不能靠复制**：Rhino 8 没有显示模式磁盘目录，必须调用 `Rhino.Display.DisplayModeDescription.ImportFromFile` 导入 `.ini`。把 `.ini` 拷到任何位置都不会生效。
- **不要凭猜测创建命名**：图层、材质、图块名称都按指引的语法生成；确实缺失的分类可按语法新建，其余情况放到语义最接近的节点并在交付说明中标注。

## 判断标准

**可读优先于合规。** 这条优先于本仓库其他所有形式约定。

- 形式偏差（大小写、字段简写、段数、字段顺序）只要语义清楚，就不要"修正"，更不要为了格式整齐而批量改名。
- **唯一需要返工的情况**：名字已经读不懂了 —— 字段粘连（如 `woodenfinishing`）、段分隔符缺失、明显笔误。
- 拿不准时保留原样并说明，不要替使用者做形式统一。

## 交付时报告

每次改动后报告：对象数量、所在图层、使用的材质与图块名。便于人工核对。
